<h1>アカウント詳細ページ</h1>

<p>
  <strong><%= User.human_attribute_name(:name) %>:</strong>
  <%= @user.name %>
</p>

<p>
  <strong><%= User.human_attribute_name(:email) %>:</strong>
  <%= @user.email %>
</p>

<%= link_to "編集", edit_user_path %> ｜
<%= link_to '削除', new_session_path, method: :delete, data: { confirm: '本当に削除しますか？' } %>